import { useState } from 'react';
import { ArrowRight, CheckCircle2, FileText, History as HistoryIcon, Target } from 'lucide-react';
import { useNavigate } from 'react-router-dom';
import { PageHeader } from '@/components/layout/DashboardLayout';
import { Button } from '@/components/ui/Button';
import { Card } from '@/components/ui/Card';
import { Badge } from '@/components/ui/Badge';
import { MOCK_JOB_HISTORY, MOCK_RESUME_HISTORY } from '@/data/mockData';

export function HistoryPage() {
  const [tab, setTab] = useState<'resume' | 'job'>('resume');
  const navigate = useNavigate();
  return <div className="max-w-6xl mx-auto"><PageHeader><p className="text-sm text-forge-400 font-medium mb-2">Your progress</p><h1 className="text-3xl sm:text-4xl font-bold font-display text-white">History</h1><p className="mt-2 text-ink-400">Review your past analyses and keep track of your growth.</p></PageHeader><div className="flex items-center gap-1 p-1 bg-ink-900 rounded-xl border border-white/5 w-fit mb-6"><button onClick={() => setTab('resume')} className={`flex items-center gap-2 px-4 py-2.5 rounded-lg text-sm font-medium transition-all ${tab === 'resume' ? 'bg-ink-700 text-white' : 'text-ink-500 hover:text-ink-200'}`}><FileText className="w-4 h-4" /> Resume analyses <span className="text-xs text-ink-500">{MOCK_RESUME_HISTORY.length}</span></button><button onClick={() => setTab('job')} className={`flex items-center gap-2 px-4 py-2.5 rounded-lg text-sm font-medium transition-all ${tab === 'job' ? 'bg-ink-700 text-white' : 'text-ink-500 hover:text-ink-200'}`}><Target className="w-4 h-4" /> Job matches <span className="text-xs text-ink-500">{MOCK_JOB_HISTORY.length}</span></button></div>{tab === 'resume' ? <Card className="p-0 overflow-hidden"><div className="hidden sm:grid grid-cols-[1fr_130px_120px_130px_110px] gap-4 px-6 py-4 border-b border-white/5 text-xs font-semibold uppercase tracking-wider text-ink-600"><span>Resume</span><span>Date</span><span>Score</span><span>Status</span><span /></div>{MOCK_RESUME_HISTORY.map((item) => <div key={item.id} className="grid sm:grid-cols-[1fr_130px_120px_130px_110px] gap-3 sm:gap-4 items-center px-5 sm:px-6 py-5 border-b border-white/5 last:border-0 hover:bg-white/[0.02] transition-colors"><div className="flex items-center gap-3"><div className="w-9 h-9 rounded-lg bg-forge-500/10 flex items-center justify-center shrink-0"><FileText className="w-4 h-4 text-forge-400" /></div><div><p className="text-sm font-medium text-white">{item.name}</p><p className="sm:hidden text-xs text-ink-500 mt-1">{item.date}</p></div></div><span className="hidden sm:block text-sm text-ink-400">{item.date}</span><span className="text-sm font-mono font-semibold text-white">{item.score}/100</span><span><Badge variant={item.score >= 85 ? 'success' : 'info'}><CheckCircle2 className="w-3 h-3" /> {item.status}</Badge></span><Button variant="ghost" size="sm" onClick={() => navigate('/resume-analyzer')}>View <ArrowRight className="w-3 h-3" /></Button></div>)}</Card> : <Card className="p-0 overflow-hidden"><div className="hidden sm:grid grid-cols-[1.2fr_1fr_130px_120px_110px] gap-4 px-6 py-4 border-b border-white/5 text-xs font-semibold uppercase tracking-wider text-ink-600"><span>Role</span><span>Resume</span><span>Date</span><span>Match</span><span /></div>{MOCK_JOB_HISTORY.map((item) => <div key={item.id} className="grid sm:grid-cols-[1.2fr_1fr_130px_120px_110px] gap-3 sm:gap-4 items-center px-5 sm:px-6 py-5 border-b border-white/5 last:border-0 hover:bg-white/[0.02] transition-colors"><div className="flex items-center gap-3"><div className="w-9 h-9 rounded-lg bg-blue-500/10 flex items-center justify-center shrink-0"><Target className="w-4 h-4 text-blue-400" /></div><div><p className="text-sm font-medium text-white">{item.jobTitle}</p><p className="sm:hidden text-xs text-ink-500 mt-1">{item.date}</p></div></div><span className="hidden sm:block text-sm text-ink-400 truncate">{item.resumeName}</span><span className="hidden sm:block text-sm text-ink-400">{item.date}</span><span className="text-sm font-mono font-semibold text-white">{item.score}%</span><Button variant="ghost" size="sm" onClick={() => navigate('/job-match')}>View <ArrowRight className="w-3 h-3" /></Button></div>)}</Card>}<div className="mt-6 flex items-center gap-3 p-4 rounded-xl bg-ink-900/40 border border-white/5 text-sm text-ink-500"><HistoryIcon className="w-4 h-4 text-ink-600" />History is stored locally in this demo and will be ready to connect to your account later.</div></div>;
}
